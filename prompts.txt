write a simple sync playwright python script for ekantipur that saves output.json in the exact required format with entertainment_news and cartoon_of_the_day
implement entertainment extraction from https://ekantipur.com/entertainment and return top 5 items with title image_url category author
use selectors based on .category blocks for entertainment cards and extract title from h2 a, image from img src or data-src, author from .author-name a
set category to मनोरञ्जन if category text is missing and set author to none if not found
implement cartoon of the day extraction from homepage section that has h4 link to /cartoon and get title from first slide img alt and image_url from src or data-src
add tiny helper functions for clean text and absolute url join, keep them very small
add basic timeout handling so failures do not crash and still write valid json schema
keep code short and readable, no classes, no advanced patterns, only essential comments
simplify this function, reduce nesting, keep output same
remove extra comments and keep only important ones
